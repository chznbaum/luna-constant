<div class="container">
  <% provide(:title, "New Photo") %>
  <h1><%= yield(:title) %></h1>

  <%= render 'form', photo: @photo %>

  <%= link_to 'Back', photos_path, class: 'btn btn-default' %>
</div>