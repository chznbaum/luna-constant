<figure>
<%= image_tag @photo.photo_upload %>
  <figcaption>
    <%= @photo.caption %> | Posted <%= distance_of_time_in_words(@photo.created_at, Time.now) %> ago by <%= @photo.user.email %>
  </figcaption>
</figure>
<%= link_to 'Edit', edit_photo_path(@photo) %> |
<%= link_to 'Back', photos_path %>
